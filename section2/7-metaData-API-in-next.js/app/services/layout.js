export const metadata = {
  title: "Services",
};

export default function ServicesLayout({ children }) {
  return (
    <main>
      <section>
        <h3>Services Layout</h3>
        {children}
      </section>
    </main>
  );
}
